DATABASE_URL="postgresql://user:password@host/db?sslmode=require"
JWT_SECRET="change-me-in-prod-please"
JWT_EXPIRES_IN="7d"
CORS_ORIGIN="http://localhost:5173"
PORT=3000
ADMIN_EMAIL="admin@liga-arauco.cl"
ADMIN_PASSWORD="changeme123"

# Nuestro Momento (spec 2026-05-004) — integración de galerías de fotos por partido.
# Base URL del deploy de NM (dev local: http://localhost:3000; prod: https://nuestro-momento.cl).
NM_API_BASE_URL="http://localhost:3000"
# API key emitida por scripts/seed-liga-arauco.ts del repo NM. Nunca se commitea.
NM_API_KEY=""
# Slug de la org en NM (informativo, no se usa en el cliente pero documenta el destino).
NM_ORG_SLUG="liga-arauco"
